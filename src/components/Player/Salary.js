import React from 'react';

const Salary = (props) => {
    const selectedPlayer = props.selectedPlayer;
     
    return (
        <div>
            <h2> </h2>
        </div>
    );
};

export default Salary;